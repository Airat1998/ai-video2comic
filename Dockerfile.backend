FROM python:3.10-slim

WORKDIR /app

COPY backend /app/backend
COPY worker /app/worker
COPY pdf /app/pdf

RUN pip install --no-cache-dir fastapi uvicorn celery[redis] redis python-multipart

ENV PYTHONPATH=/app

CMD ["uvicorn", "backend.main:app", "--host", "0.0.0.0", "--port", "8000"]
